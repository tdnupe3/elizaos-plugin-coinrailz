node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# XMTP Database Files
*.db3
*.db3-wal
*.db3-shm
*sqlcipher*
xmtp-dev-*
xmtp-production-*
.xmtp-*

# Development artifacts
.env.local
*.log

# Environment Files - NEVER COMMIT
.env
*.env
!.env.example
!.env.production.template

# Security - NEVER COMMIT
**/*private*key*
**/*seed*
**/*mnemonic*
**/wallet*.json
cdp_*.json

# CDP Credential Files
attached_assets/cdp_api_key_*.json
*.json
# Build and typecheck configuration must remain reproducible.
!/package.json
!/package-lock.json
!/tsconfig.json
# Publishable ElizaOS plugin metadata and compiler settings are release inputs.
!/elizaos-plugin-coinrailz/package.json
!/elizaos-plugin-coinrailz/tsconfig.json
!/elizaos-plugin-coinrailz/tsconfig.test.json
# Canonical OpenAPI spec — must be versioned (not a credential)
!public/openapi-x402-services.json
# Drizzle migration history is reproducible build metadata, not secret data.
!migrations/meta/
!migrations/meta/_journal.json
!migrations/meta/*_snapshot.json

# Coin Railz Platform Assessment
**Window:** 2026-08-08 13:58 UTC → 2026-08-09 01:58 UTC (12 hours)
**Comparison:** 2026-08-07 13:58 UTC → 2026-08-08 01:58 UTC (same 12-hour band, yesterday)
**Generated:** 2026-08-09 ~02:00 UTC
**Fixes active this window:** #100 (recipe endpoint), #101 (vault example bodies), #94 (facilitator), #95 (HEAD headers)

---

## 1. Executive Summary

The earthdata organic payer (92.255.110.46) converted — **$0.25 paid on earthdata-soil-moisture at 22:15 UTC** using the HEAD-then-pay pattern. This is the first external organic payment since Aug 6 and confirms the payer is actively expanding its paid service catalog rather than being a one-time event. The canary ran cleanly (3 cycles, 6 paid). Total paid in window: 8 transactions.

Traffic was down 26% in volume (1,117 vs 1,506 requests yesterday) but quality was up — the earthdata payer paid, and the IPv6 agent ran its most methodical catalog expansion session yet, individually probing B20, Robinhood Chain, RWA, and vault services between burst cycles. The IPv6 agent posted to vlt-usdc-deposit after the vault example bodies fix was live and still got 400 (wrong schema), which needs investigation.

**Verdict: Moving forward.** Organic revenue materialized, the payment rail worked cleanly, and two actors are in sustained active evaluation.

---

## 2. Headline Signals

### 1. Earthdata payer paid $0.25 on earthdata-soil-moisture (22:15 UTC)
This is the critical event of the window. 92.255.110.46 (`undici` UA) followed its standard pattern: multiple HEAD probes → payment. Specifically: 4× HEAD earthdata-soil-moisture (402) → 2× HEAD satellite-earthdata (402) → 2× HEAD earthdata-soil-moisture (200 paid). The wallet is 0x3803a192... — the same wallet that paid earthdata-precipitation and earthdata-ocean-color previously. This is not a one-time payer. It's systematically working through the earthdata service catalog, paying each service it evaluates.

**Why it matters:** Confirms the earthdata service vertical is a recurring revenue source. The payer has now paid 3 distinct earthdata services. The HEAD headers from fix #95 were live for this payment — the payer received correct price/network/recipe info on HEAD before committing.

### 2. IPv6 agent ran its longest single session — 6 hours of methodical catalog expansion
The agent (2a06:98c0:3600::103) was active from 14:05 UTC through at least 00:13 UTC (Aug 9) — nearly 10 hours with 3 burst cycles and sustained individual service probing in between. New service categories probed this session: B20 (b20-transfer-check, b20-compliance-scan, b20-token-info) at 21:46, RH services at 21:46, RWA (rwa-nav-oracle, tokenized-yield-compare) at 23:04, Robinhood + instant-api-key at 23:32, and vlt-usdc-deposit at 23:56.

**Why it matters:** The agent is now past the discovery phase and into individual service evaluation. It's expanding from DeFi core to blockchain data feeds (B20, RH) and vault services. This is deeper funnel behavior than any prior session.

### 3. IPv6 agent POSTed vlt-usdc-deposit again — still 400 after fix
At 23:56 UTC, the agent GET'd vlt-usdc-deposit (received the GET response with correct body schema) then immediately POSTed and got 400. Fix #101 was live since ~16:30 UTC, so the GET response contained the correct example body. The 400 means the agent is ignoring the exampleBody in the GET response and sending its own (wrong) body schema. This is a different failure mode than missing params — the agent may be constructing its own body from the GET-endpoint documentation instead of using the example.

**Why it matters:** The fix is live but the agent isn't using the example body. The vault POST 400 won't be resolved by the example bodies fix alone — the agent needs to consume the example or the GET docs need to be more explicit about exact field format.

### 4. New canary IP (136.124.32.83) — canary now running from two IPs
A `node` UA at 136.124.32.83 ran the complete HEAD → GET → POST payment sequence twice in the window (16:16 UTC and 22:15 UTC), paying 2× each time (4 paid total). Wallet 0x5837a864c... is the same canary wallet as 34.96.45.252. The canary system appears to have been updated or load-balanced to a second IP.

**Why it matters:** Canary is running from an additional node. The payment rail (Base mainnet) confirmed clean at 14:23 (34.96.45.252), 16:16, and 22:15 UTC (both 136.124.32.83).

### 5. Earthdata payer also probed iot-sensor-reading (17:15 UTC) — new service
At 17:15 UTC, 92.255.110.46 did 2× HEAD on iot-sensor-reading (402). It didn't pay there — it came back later and paid earthdata-soil-moisture instead. The payer is evaluating multiple service verticals per session (IoT, satellite data). Historical pattern: HEAD → 1–3 day delay → pay. iot-sensor-reading is now in its evaluation queue.

**Why it matters:** The earthdata payer is expanding beyond the earthdata vertical. IoT sensor data at $0.10 could be the next target.

### 6. Traffic volume down 26% but concentrated in fewer, more productive actors
1,117 vs 1,506 requests yesterday. The drop is primarily reduced x402-observer activity (525 hits vs 555 yesterday) and reduced python-httpx volume (480 vs 560). The important actors — earthdata payer and IPv6 agent — maintained or increased their activity. Quality beats quantity.

### 7. ScoutScore-HealthCheck/1.0 — new ecosystem health monitor
13.223.234.192 (AWS) HEAD-probed token-metadata at 23:30 UTC. First appearance. Another x402 ecosystem health monitoring system is tracking the platform.

---

## 3. Actor Analysis

### Canary / Internal
**34.96.45.252 (node) — canary primary**
- 8 hits, 4 POSTs, 2 HEADs, 6 challenges, **2 paid** (first-call at 14:23 UTC)
- Standard cycle. Rail confirmed clean.

**136.124.32.83 (node) — canary secondary (NEW IP)**
- 16 hits, 8 POSTs, 4 HEADs, 12 challenges, **4 paid** (first-call at 16:16 and 22:15 UTC)
- Full HEAD → GET → POST → pay sequence twice. Canary system expanded to second IP.

### Major AI Platforms / Autonomous Agents

**IPv6 agent — 2a06:98c0:3600::103 (blank UA, Cloudflare network)**
- Active 14:05–00:13 UTC (≥10 hours), 3 burst cycles, extended between-cycle probing
- Session 1 (14:05): DeFi services burst, then GET /discovery + GET /catalog
- Individual probes (16:24–17:44): trade-signals, token-sentiment, whale-alerts, trending-tokens, dex-liquidity, transaction-builder, token-price, prediction-market-spread
- Session 2 (19:09): DeFi services burst, then GET /discovery
- Individual probes (21:46–23:56): B20 services, RH services, RWA services, Robinhood services, vlt-usdc-deposit
- Session 3 (00:13): DeFi services burst (window cut off)
- vlt-usdc-deposit: GET 200 at 23:56, POST 400 at 23:56 — still sending wrong body
- 0 payments. Biz dev assessment: ~10% probability in next 48h; likely a compatibility harness or funded-wallet issue, not a catalog info gap.

**Earthdata payer — 92.255.110.46 (undici)**
- 10 hits, 0 POSTs, 10 HEADs, 8 challenges, **2 paid** (earthdata-soil-moisture at 22:15 UTC)
- 17:15: 2× HEAD iot-sensor-reading (402) — new service probe
- 22:15: 4× HEAD earthdata-soil-moisture (402) → 2× HEAD satellite-earthdata (402) → 2× HEAD earthdata-soil-moisture (200 paid)
- Pattern: HEAD probe new service → come back later for the payment → move on to next
- Classification: **Established recurring organic payer.** 3 distinct earthdata services paid now.

### SEO / Research Bots

**x402-observer — 2.208.198.190 (x402.fuchss.app)**
- 525 hits, 62 POSTs, 48 services covered, 525 challenges. Standard monitor.

**python-httpx — 163.47.70.38**
- 480 hits, 0 POSTs, 20 curated services, GET-only. Identity still unknown.

**SERankingBacklinksBot — 144.76.32.190**
- 11 hits, 8 services. Standard SEO backlink crawler.

**AhrefsBot — 148.113.128.24**
- 1 hit (rh-bridge-usdc). Routine.

**SemrushBot — 185.191.171.x**
- 4 hits across 3 IPs: solana-yield-finder, ai-inference, payment-processing, first-call. First-call returned 200 (no payment required on GET, returned challenge but SemrushBot got it). Normal crawl behavior.

### Unknown Recurring Actors

**34.158.104.72 (node, GCP)**
- 8 hits, 0 POSTs, 8 HEADs across ping, token-metadata, gas-price-oracle
- Hourly HEAD cadence. Systematic service health monitor. Has appeared across multiple windows.
- Pattern suggests a monitoring tool tracking 3 specific services.

### New Actors

**ScoutScore-HealthCheck/1.0 — 13.223.234.192 (AWS)**
- 1 HEAD on token-metadata at 23:30 UTC. First appearance.
- UA structure matches ecosystem health monitoring bots (x402-healthbot, Cinderwright-HealthCheck pattern).

**Cinderwright-HealthCheck/1.0 — 89.167.115.8**
- GET /catalog (200) + GET /gas-price-oracle%60 (404). The %60 is a percent-encoded backtick — known issue, catch-all handles it correctly with did_you_mean.

### Bazaar / Crawlers

**meta-externalagent/1.1 — 57.141.0.x (Facebook)**
- 8 hits across 7 IPs: approval-manager, token-metadata, polymarket-odds, dex-liquidity. Reading service pages for social graph indexing.

**ShapBot — 34.44.196.215**
- 2 hits: HEAD + GET on catalog (both 200). Reading the catalog index.

**x402-healthbot/1.0 — 54.242.133.170 (decixa.ai)**
- 1 hit: stock-sentiment (402). Ecosystem health check.

### Depleted Wallet Actors (not bugs)

**74.220.48.55 (node)**
- 28 hits, 28 POSTs, all 402. Known depleted USDC wallet. Not a platform issue.

---

## 4. Endpoint Demand Analysis

### Top Services by Hits
| Service | Hits | POSTs | HEADs | Paid | Notes |
|---------|------|-------|-------|------|-------|
| compliance-consultation | 47 | 16 | 0 | 0 | Observer + depleted-wallet actor |
| instant-agent-wallet | 40 | 24 | 0 | 0 | High POST volume |
| ping | 35 | 0 | 3 | 0 | HEAD monitor (34.158.104.72) |
| first-call | 25 | 12 | 6 | **6** | Canary (3 cycles) |
| earthdata-soil-moisture | ~10 | 0 | 10 | **2** | Earthdata payer paid here |
| trade-signals | 22 | 14 | 0 | 0 | High POST volume |
| token-metadata | 19 | 0 | 4 | 0 | GCP monitor + ScoutScore HEAD |
| gas-price-oracle | 18 | 0 | 2 | 0 | GCP monitor HEAD |

### Services With Meaningful Attention
- **earthdata-soil-moisture**: Converted to paid. Also probed alongside satellite-earthdata. Earthdata vertical in active use.
- **compliance-consultation**: 16 POSTs from 2 actors (observer + depleted wallet). Genuine POST demand.
- **instant-agent-wallet**: 24 POSTs, 3 actors. High real POST interest.
- **iot-sensor-reading**: Earthdata payer HEAD'd at 17:15. In evaluation queue.
- **vlt-usdc-deposit**: IPv6 agent GET+POST at 23:56. Second time this service has been actively attempted.

### Most Likely to Convert Next
1. **iot-sensor-reading ($0.10)**: Earthdata payer HEAD-probed it 5+ hours before the earthdata-soil-moisture payment. Historical lag 1–3 days. Window: Aug 9–11.
2. **satellite-earthdata**: Probed alongside earthdata-soil-moisture at 22:15. The payer probed it but paid soil-moisture first. Next session likely revisits.
3. **earthdata-granules / earthdata-sst / earthdata-ocean-color**: The payer is working through the earthdata catalog systematically. Any of these could be next.
4. **multi-chain-balance**: Probed heavily in prior window (Aug 8 morning). Still in the evaluation horizon.

---

## 5. Impact of Recent Updates / Fixes

### #95 — HEAD-visible payment headers
**Working as intended.** Earthdata payer received correct price/network/recipe headers on HEAD before paying earthdata-soil-moisture. The 4× HEAD retry before submitting payment is the standard pattern — the agent reads headers, evaluates, submits. This fix is contributing to conversion readiness.

**X-Payment-Recipe-URL** (fix #100): No evidence yet of an agent specifically calling the recipe endpoint in the window. The header is correct and live.

### #101 — Vault example bodies
**Not helping yet.** IPv6 agent POSTed vlt-usdc-deposit at 23:56 and still got 400. The GET response at 23:56 (200) contained the correct example body, but the agent's subsequent POST still sent wrong schema. The agent is constructing its own body, not copying the example. The fix improves the 402 body for agents that read it, but this particular agent's vault POST failure is in its own request construction logic, not in the platform's documentation.

### #94 — Facilitator field in accepts[]
**Live, holding.** No malformed 402s logged. Earthdata payer's successful payment at 22:15 confirms the full accepts[] chain (scheme, network, maxAmountRequired, payTo, facilitator) is working end-to-end for Base USDC payments.

### Canary rail
**Clean.** 3 successful canary cycles (14:23, 16:16, 22:15 UTC). Base mainnet settlement confirmed working across both canary IPs.

---

## 6. Conversion Readiness

**External organic payment this window: $0.25 (earthdata-soil-moisture, 22:15 UTC)** ✅

Canary: 6 paid calls across 3 cycles. Base rail clean.

All-time from x402_payment_intents: Use the live table — do not cite the x402_interactions aggregate which under-counts.

**Funnel state:**

| Actor | Stage | Assessment |
|-------|-------|------------|
| Earthdata payer (92.255.110.46) | **Active payer** — paid this window | Recurring; expect further conversions as it expands catalog |
| IPv6 agent (2a06:98c0:3600::103) | Sustained POST bursts + individual service probing | 10% payment probability next 48h per biz dev; wallet/execution issue most likely blocker |
| iot-sensor-reading (earthdata payer queue) | HEAD-probed; 1–3 day lag | Aug 9–11 conversion window |
| satellite-earthdata (earthdata payer) | Probed alongside paid service | Next evaluation cycle likely |

**Positive signals:**
- Organic payment confirmed in window — the HEAD-visible headers from fix #95 were present for this conversion
- Earthdata payer is systematically working through the catalog (not a one-time event)
- IPv6 agent now probing vault and transaction-type services specifically

---

## 7. Security / Technical Issues

**400 errors:**
- `vlt-usdc-deposit POST 400` at 23:56 (IPv6 agent). Agent is sending wrong body. Not a platform bug — the service correctly validates and rejects.

**404 errors:**
- `gas-price-oracle%60 GET 404` (Cinderwright-HealthCheck). Percent-encoded backtick. Catch-all's `normalizeSlug` should handle this — the 404 suggests the slug normalization didn't match. Minor; no commercial impact.

**No 500 errors.** No rate limit violations. No security probes of concern.

**Architect note on 402 body size:** As fix #95 added headers and fix #101 added example bodies, the per-402 payload is now consistently larger. No latency or size issues observed in this window, but architect recommends monitoring p95 TTFB on 402 responses as the payload grows.

---

## 8. Business Development Read

**This window's commercial read:** The earthdata payer is the most important actor on the platform right now. It paid a third service ($0.25 on earthdata-soil-moisture), confirming it's a repeat buyer, not a curiosity visitor. The HEAD-then-pay pattern is established. The biz dev verdict is watch-only — the payer is already in motion, outreach would interfere.

**earthdata payer trajectory:** 
- Paid: earthdata-precipitation ($0.25), earthdata-ocean-color ($0.25), earthdata-soil-moisture ($0.25)
- Probed this window: iot-sensor-reading, satellite-earthdata
- Likely next: iot-sensor-reading, satellite-earthdata, earthdata-granules, earthdata-sst
- Potential catalog value: 6–8 earthdata services × $0.25 = $1.50–$2.00/session if it cycles through all

**IPv6 agent:** Biz dev assessment is 10% conversion probability in 48h. The 10-hour session length and expanded service probing (B20, vault, RWA) suggest genuine evaluation, but three sessions of POST bursts without payment is strong negative evidence. Most likely blocker: wallet funding or payment execution policy in the agent's system. Not actionable from the platform side.

**hermes-contact-discovery:** Still not contacted. Biz dev says reach out now — the context window from their Aug 8 sweep is still fresh. Send a concise email to contact@hermes.ai. This remains the best actionable lead.

**Overall 7-day prognosis:** Modestly positive. Earthdata payer is a confirmed repeat buyer and will likely pay 1–2 more services within a week. Canary rail is clean. IPv6 agent remains a low-probability upside. Base case: $0.25–$0.75 additional organic revenue this week from earthdata services alone.

---

## 9. Action Items

1. **Send outreach to hermes-contact-discovery (contact@hermes.ai) — do it today.** Their Aug 8 sweep is now 12+ hours old. One concise email: acknowledge the 48-service scan, highlight enterprise compliance/wallet-risk/DeFi services, offer concrete x402 integration examples. One follow-up maximum.

2. **Investigate IPv6 agent vlt-usdc-deposit 400 — what body is it sending?** The GET response contains the correct example. But the agent POSTed wrong body anyway. Check if the GET endpoint's `body` documentation format matches what the agent constructs. The issue may be the field descriptions (e.g., `'string — total USDC to deposit (e.g. "100" for 100 USDC)'`) being parsed literally rather than the example being used.

3. **Track earthdata payer's next probe.** Monitor for HEAD requests from 92.255.110.46 on iot-sensor-reading and satellite-earthdata specifically. When it HEAD-probes → expect payment within 72h. No action needed — just awareness.

4. **Verify the recipe endpoint URL is correct in production.** The dev workflow showed `http://workspace.traviskellogg1.repl.co/...` as the base URL instead of `https://coinrailz.com`. Confirm `X-Payment-Recipe-URL` in production 402 responses shows `coinrailz.com` (not the dev domain).

5. **Log the earthdata payer payment in the organic revenue tracker.** All-time revenue should be updated. The $0.25 earthdata-soil-moisture payment at 22:15 UTC Aug 8 is a confirmed new transaction from wallet 0x3803a192...

6. **Do not attempt to identify or outreach the python-httpx actor (163.47.70.38).** It has been running for 10+ windows with a curated 20-service list and zero POST activity. Classify as infrastructure monitor, stop tracking as a conversion candidate.

7. **Cinderwright-HealthCheck 404 on gas-price-oracle%60:** The `normalizeSlug` function strips trailing backtick (`` ` ``) but returned 404 instead of 404-with-did_you_mean. Confirm the did_you_mean hint was included in the 404 body. If not, check that the normalizeSlug strip is applied before bestMatch lookup.

8. **Add earthdata-soil-moisture and iot-sensor-reading to the SEMANTIC_PATTERNS boost** in the A2A routing if not already present. The earthdata payer's ongoing conversion in these services confirms demand is real and should be surfaced to A2A agents querying for data services.

---

## 10. Final Verdict

Organic revenue materialized in this window. The earthdata payer is a confirmed multi-service recurring buyer — the most valuable actor on the platform by revealed preference. The canary ran clean across two IPs. The IPv6 agent is in its deepest evaluation session yet but has not converted despite multiple correct 402 bodies.

The platform is moving forward. The HEAD payment pattern from fix #95 had its first confirmed conversion in this window. The recipe endpoint (#100) and vault example bodies (#101) are live but have not yet produced measurable impact — the earthdata payer didn't need them (it's already paying), and the IPv6 agent isn't using the vault examples.

Priority for the next 24 hours: contact hermes, watch iot-sensor-reading, and investigate why the IPv6 agent's vault POST body is still wrong.

**Confidence: High** — earthdata payment is confirmed in the data, canary data is unambiguous, all actor patterns are consistent with prior windows.
